# Process/network boundaries — never shrink these
app.anki.sync_orchestrator.subprocess.run       # subprocess boundary (inside _run_driver)
app.anki.sync_orchestrator._keychain_password   # macOS Keychain boundary
app.audio.edge_tts.edge_tts.*                   # EdgeTTS network boundary (Communicate, list_voices)
app.audio.edge_tts.asyncio.sleep                # EdgeTTS retry sleep
app.anki.media.pixabay.*                        # Pixabay HTTP client boundary
app.anki.media.forvo._make_client               # Forvo HTTP client boundary
app.api.anki.fetch_card_media                   # Pixabay/Forvo network boundary (card media fetch)
# The Settings singleton pinned through any module's import of it
app.*.settings.*
# Path-constant pins — test infrastructure writes to temp dirs
app.anki.sync._MEDIA_DIR
app.api.srs._MEDIA_DIR
app.anki.media.vocab_media._MEDIA_DIR
app.anki.model_discovery._CACHE_PATH
# Cached-instance pin (deterministic test defaults)
app.api.srs._lemmatizer
