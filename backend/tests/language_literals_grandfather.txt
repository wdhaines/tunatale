# Shrink-only ledger of pre-existing hardcoded-language literals found in
# app/**/*.py outside the sanctioned plugin/registry modules (see
# tests/language_literals_allowlist.txt). Each row is a seam that should
# eventually route through app/languages.py / the language-plugin system
# instead of a bare literal in general-purpose code.
#
# Format: file<TAB>literal<TAB>count (literal is escaped: \\ \t \n \r).
# Counts may only go DOWN over time (as seams get refactored away) — never
# add a new line here or bump a count up. Regenerate with:
#   uv run python scripts/check_language_literals.py --write-grandfather
# and re-apply this header, or diff by hand to confirm only shrinkage.
# NOTE: pixabay.py "no" is the English word "no" (dictionary key for image
# queries), NOT the Norwegian language code — a checker false positive.
app/cards/media/pixabay.py	no	1  # reason: PERMANENT — English word "no" (dict key for image search terms), not the Norwegian code; checker false positive, not drainable
app/plugins/anki_sync/sqlite_reader.py	class="slovene"[^>]*>\\s*([^<]+?)\\s*<	2  # reason: DEFER — inside Anki safety envelope, orchestrator-owned
app/plugins/anki_sync/sync.py	sl	1  # reason: DEFER — inside Anki safety envelope, orchestrator-owned
app/plugins/anki_sync/sync_writer.py	sl	2  # reason: DEFER — inside Anki safety envelope, orchestrator-owned
