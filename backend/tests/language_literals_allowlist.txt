# Sanctioned plugin/registry homes where hardcoded language literals
# legitimately live. TunaTale is language-plugin based ("No hardcoded
# language logic" in root CLAUDE.md) — these are the modules that ARE the
# plugin/registry layer, so language codes, names, engine names, and TTS
# voice ids are expected and exempt from the checker.
#
# A file matching one of these globs is fully exempt: every hit in it is
# skipped, and it never enters the grandfather ledger.
app/languages.py
app/models/language.py
app/audio/preprocessing/*.py
app/anki/vocab_notetype.py
app/anki/add_vocab_notetype.py
app/srs/lemmatizer.py
app/config.py
# Phonotactics plugin home: its per-language onset inventories hold consonant
# clusters like "sl"/"nb" that are NOT language codes, alongside the code→syllabifier
# dispatch table — the syllabification counterpart to audio/preprocessing/.
app/generation/syllabify.py
# Anki-schema mapping home: maps extraction roles to specific Anki notetype +
# FIELD NAMES (e.g. the "6000 Most Frequent Norwegian Words" notetype's "Norwegian
# word" field). Those are external Anki schema strings, like vocab_notetype.py.
app/anki/field_map.py
