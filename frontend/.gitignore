node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/.svelte-kit-test
/.svelte-kit-e2e
/.svelte-kit-e2e-1
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Coverage (generated by `bun run test:coverage` → consumed by scripts/coverage-gate.ts)
/coverage

# Wake-word probe assets (tunatale-mkyb) — ~19 MB of pinned, immutable release
# artifacts fetched by static/voice-probe/fetch-assets.sh, plus the TTS stimuli
# check-probe.mjs generates. The probe's preflight reports when they are absent
# and prints the command, so a fresh clone gets an instruction, not a mystery.
/static/voice-probe/models/
/static/voice-probe/ort/
/static/voice-probe/stimuli/
