// Editor + `bun run build`. Layers the shared options over the tree that
// `svelte-kit sync` generates at the DEFAULT outDir. Array `extends` is
// later-wins and needs TypeScript >= 5.0 (this repo is on 6.x).
{
	"extends": ["./.svelte-kit/tsconfig.json", "./tsconfig.base.json"]
}
