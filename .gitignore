# Prototype directories — each has its own git repo
micro-demo-0.0/
micro-demo-0.1/

# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
.eggs/
dist/
build/

# Virtual environments
.venv/
venv/
env/

# Environment / secrets
.env
.env.*
!.env.example

# SSL certs (local dev only)
certs/
*.pem

# Test & coverage artifacts
.coverage
coverage.xml
coverage.json
htmlcov/
.pytest_cache/
.mypy_cache/
*.py,cover

# Node / frontend
node_modules/
frontend/.svelte-kit/
frontend/build/

# Claude Code: team-shared rules + settings.json are tracked.
# Only per-user / scratch files stay local.
.claude/settings.local.json
.claude/plans/
.claude/scheduled_tasks.lock

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log

# Runtime data / DB
*.db
*.db-journal
*.db-shm
*.db-wal
*.db.bak*
*.db.pre_*

# TTS / audio cache
backend/cache/

# Imported Anki media (per-user, large)
backend/media/

# Generated outputs. (Ad-hoc script ignores moved to backend/scripts/.gitignore
# — a root-level "backend/scripts/*" glob broke ruff's local file discovery.)
backend/output/
frontend/test-results/

# Agent handoff briefs — session-scoped working docs, never committed
docs/briefs/
